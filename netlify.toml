[build]
command = "npm run build"
publish = "dist"

[[redirects]]
from = "/*"
to = "/index.html"
status = 200

[context.production]
command = "npm run build"
publish = "dist"

[context.develop]
command = "npm run dev"
publish = "dist"

# Note: Backend API is now handled by Firebase
# No backend server needed - everything runs client-side
